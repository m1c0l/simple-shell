#!/usr/local/cs/execline-2.1.4.5/bin/execlineb

time -p
background {
  time -p 
  redirfd -r 0 out
  redirfd -w 1 /dev/null
  getpid one sort
}
background {
  time -p 
  redirfd -r 0 out
  redirfd -w 1 /dev/null
  sort
}
background {
  time -p 
  redirfd -r 0 out
  redirfd -w 1 /dev/null
  sort
}
background {
  time -p 
  redirfd -r 0 out
  redirfd -w 1 /dev/null
  sort
}
